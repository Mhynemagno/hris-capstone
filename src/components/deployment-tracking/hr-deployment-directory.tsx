"use client";
import Link from "next/link";
import { ErrorState } from "@/components/ui/error-state";
import { LoadingState } from "@/components/ui/loading-state";
import { useHrDeployments } from "@/hooks/use-deployment-tracking";
import { DeploymentStatusBadge } from "./deployment-status-badge";
export function HrDeploymentDirectory() { const query = useHrDeployments({ page: 1, pageSize: 25 }); if (query.isLoading) return <LoadingState label="Loading deployments…" />; if (query.error) return <ErrorState message={query.error.message} />; const rows = query.data?.rows ?? []; return <section className="space-y-4"><div className="flex justify-end"><Link className="rounded-lg bg-primary px-4 py-2 text-sm font-medium text-primary-foreground" href="/hr/deployments/new">New deployment</Link></div>{rows.length ? <div className="overflow-x-auto rounded-xl border"><table className="w-full min-w-[700px] text-sm"><thead className="bg-muted"><tr><th className="p-3 text-left">Role</th><th className="p-3 text-left">Destination</th><th className="p-3 text-left">Dates</th><th className="p-3 text-left">Status</th><th className="p-3 text-right">Details</th></tr></thead><tbody>{rows.map((row) => <tr className="border-t" key={row.id}><td className="p-3">{row.assignment_role}</td><td className="p-3">{[row.location, row.unit, row.project].filter(Boolean).join(" · ")}</td><td className="p-3">{row.starts_on} – {row.ends_on ?? "ongoing"}</td><td className="p-3"><DeploymentStatusBadge status={row.status} /></td><td className="p-3 text-right"><Link className="underline" href={`/hr/deployments/${row.id}`}>Details</Link></td></tr>)}</tbody></table></div> : <p className="rounded-xl border p-4 text-sm text-muted-foreground">No deployments found.</p>}</section>; }
